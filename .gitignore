# build output
dist/
.output/

# dependencies
node_modules/

# astro
.astro/

# tinacms: tina/tina-lock.json (Schema-Lock) MUSS committet werden — Tina Cloud
# liest sie, um den Branch zu indexieren. Der __generated__-Ordner wird komplett
# ignoriert (wird bei `tinacms dev`/`build` automatisch neu erzeugt).
tina/__generated__/
public/admin/

# env / secrets — Tina Cloud clientId & token gehören hier rein, NICHT ins Repo
.env
.env.*
!.env.example

# logs / os
npm-debug.log*
.DS_Store
